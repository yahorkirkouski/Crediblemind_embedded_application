import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
  button: {
    padding: 0,
    marginLeft: '8px',
    textTransform: 'none',
    fontSize: 'inherit',
    fontWeight: 'normal',
    verticalAlign: 'baseline',
  },
});
