import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
  error: {
    background: 'hsla(355, 98%, 66%, 0.4)',
    border: '1px solid red',
    borderRadius: 3,
    boxShadow: '0 3px 5px 2px rgba(255, 105, 135, .3)',
    color: 'red',
    height: 48,
    fontSize: 20,
    padding: '0 30px',
  },
});
